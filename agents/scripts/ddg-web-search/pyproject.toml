[project]
name = "ddg-web-search"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4",
  "lxml",
  "httpx",
  "python-dotenv",
  "mcp",
  "html-to-markdown",
  "curl_cffi",
]
